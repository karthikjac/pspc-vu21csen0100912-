// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
int a=2;
int b=1;
for(a=2;a<20;++a)
{
printf("\n %d",++a);
}
for(b=1;b<20;++b)
{
printf("\n %d",++b);    
}

    return 0;
}
