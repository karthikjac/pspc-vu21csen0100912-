#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    int len=0,i;
    printf("enter the string");
    gets(a);
    for(i=0;a[i]!=0;i++)
    {
        len=len+1;
    }
    printf("the length of string is:%d",len);
    return 0;
}
