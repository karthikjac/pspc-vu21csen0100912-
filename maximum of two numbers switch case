#include <stdio.h>
int main()
{
  int num1,num2;
  printf("enter two numbers to find maximum");
  scanf("%d%d",&num1,&num2);
  switch(num1>num2)
  {
  	
    case 1:
    printf("%d is maximum",num1);
    break;
    case 2:
    printf("%d is maximum",num2);
    break;
  }
    return 0;
}
    
